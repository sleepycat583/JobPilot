本项目严格按照 /docs/09-求职多智能体辅助系统架构设计.md 执行开发。

规则：
1. 任何代码实现前，先确认对应章节的 Schema、职责边界和验收标准，不要自行发挥字段名或接口设计。
2. 严禁在未完成第 13/14 章 Week1-2 核心验收前，接入 React、SQLAlchemy 或 Docker。
3. 每次提交前，commit message 按文档第19.2节的三段式格式：改了什么/为什么改/排除了什么方案（仅在偏离已冻结设计时需要，正常按计划推进的commit不需要）。
4. 不新增技术栈、不新增 Agent、不把 Tool 改成 Agent，除非文档第19章的"重大问题"条件成立。